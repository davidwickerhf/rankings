# Archived Data

This directory contains historical data exports and intermediate tables that
were previously mixed into `data/`.

## Current Source Of Truth

- `data/METADATA/echr_metadata.csv`

Everything here is preserved for traceability only. Active derived products
should instead be written to `results/load/` or retained under the fixed
network/result directories already used by the paper-facing pipeline.
